<div class="breadcrumbs">
    <div class="breadcrumbs__i">
        <ul>
            {% if breadcrumbs.0.name is defined and breadcrumbs.0.name != "TM Sirius" and breadcrumbs.0.name != "Результаты поиска" %}
                <li><a href="{{ url('category', {'category': 'all'}) }}">{% trans %}TM Sirius{% endtrans %}</a></li>
            {% else %}
                <li><a href="{{ url('homepage') }}">{% trans %}To homepage{% endtrans %}</a></li>
            {% endif %}
            {% for item in breadcrumbs %}
                <li>
                    {% if item.url is defined %}
                        <a href="{{ item.url }}">{{ item.name }}</a>
                    {% else %}
                        {{ item.name }}
                    {% endif %}
                </li>
            {% endfor %}
        </ul>
    </div>
</div>