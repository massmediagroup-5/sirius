{% if orders %}
    <div class="user-orders">
        <div class="user-orders__i">
            {% for order in orders %}
                {% include 'AppAdminBundle:admin/orders/order.html.twig' with {order: order} %}
            {% endfor %}
        </div>
    </div>
{% else %}
    <div class="filter-no-results lk-opt">
        <p>У Вас пока нет заказов</p>
    </div>
{% endif %}